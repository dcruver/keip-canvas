<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:integration="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           https://www.springframework.org/schema/integration/spring-integration.xsd
">

    <integration:inbound-channel-adapter id="messageGenerator"
                                         channel="messageSource"
                                         expression="'TestMessage'">
        <integration:poller fixed-rate="5000">
            <integration:advice-chain>
                <integration:ref bean="adviceRef"/>
            </integration:advice-chain>
        </integration:poller>

        <integration:expression key="testExp"/>
    </integration:inbound-channel-adapter>

    <integration:channel id="messageSource"/>

    <integration:channel id="resultChannel"/>

    <integration:filter id="checkPayload"
                        expression="payload == 'error'"
                        input-channel="messageSource"
                        output-channel="resultChannel"
                        discard-channel="logChannel"/>

    <integration:outbound-channel-adapter id="resultSender" channel="resultChannel"/>

    <integration:logging-channel-adapter id="testLogger" channel="logChannel"/>

    <integration:channel id="logChannel"/>

</beans>
