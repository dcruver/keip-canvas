Some XSDs don't conform to the spec and fail validation when parsed. The XSDs in the "patched-schemas" directory
have been copied and patched to pass validation.

Any modifications should be tagged with an XML comment:

<!--Manually Modified -->